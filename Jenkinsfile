pipeline {
  agent any
        
  stages {
	    stage('Scm Checkout') {
		    steps {
			    	checkout scm
		    }
	    }
  
	    stage('print') {
		    steps {
			    	echo "hello"
		    }
	    }
  }
}
